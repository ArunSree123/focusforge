spring:
  datasource: { url: "jdbc:h2:mem:t;MODE=PostgreSQL;DB_CLOSE_DELAY=-1", username: sa, password: "" }
  jpa: { hibernate: { ddl-auto: create-drop } }
app:
  jwt: { secret: "test-secret-test-secret-test-secret-1234", expiration-hours: 1 }
  cors: { allowed-origins: "http://localhost:5173" }
  timezone: Asia/Kolkata
